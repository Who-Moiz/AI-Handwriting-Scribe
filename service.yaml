apiVersion: v1
kind: Service
metadata:
  name: ai-scribe-service
spec:
  type: NodePort
  selector:
    app: ai-scribe
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30001